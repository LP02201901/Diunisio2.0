CLASE ABSTRACTA empleado  {
    CONSTRUCTOR empleado (a,b){
		imprimirPantalla (a+b);
    }
    def cadena set_cargo(nombre){
        nombre = nombre;
        imprimirPantalla (nombre);
    }
}

empleado enrique = NUEVO empleado((3,4));
enrique->set_cargo("Enrique");
